# TaskSchedulerManager 配置示例
# 复制此文件为 config.env 并按需修改

# 定时任务文件夹路径
# 支持绝对路径或相对于插件目录（__dirname）的相对路径
# Linux/Mac 示例（绝对路径）: TASK_DIR=/opt/vcp/VCPTimedContacts
# Windows 示例（绝对路径）:   TASK_DIR=C:/VCP/VCPTimedContacts
# 默认（相对路径）:           TASK_DIR=../../VCPTimedContacts
TASK_DIR=../../VCPTimedContacts

# 是否开启调试日志（输出到 stderr）
# DEBUG_MODE=true
DEBUG_MODE=false